import 'package:flutter/material.dart';

class AppColorScheme {
  // 🌸 Brand Colors
  static const Color primary = Color.fromARGB(255, 158, 91, 183); // Base pink
  static const Color secondary = Color.fromARGB(255, 255, 199, 245); // Soft blush for accents
  static const Color background = Color.fromARGB(255, 152, 116, 165); // Very light pink background
  static const Color surface = Color.fromARGB(255, 255, 255, 255); // Clean white surface
  static const Color border = Color(0xFFFFD1E0); // Subtle pink border
  static const Color error = Color(0xFFE53935); // Red for errors

  // 🩶 Text Colors
  static const Color textPrimary = Color(0xFF3A3A3A); // Dark gray for readability
  static const Color textSecondary = Color(0xFF757575); // Muted gray for secondary text
  static const Color textOnPrimary = Color(0xFFFFFFFF); // White text on pink buttons
}
